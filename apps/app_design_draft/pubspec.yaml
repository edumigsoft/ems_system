name: app_design_draft
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  core_shared:
    path: ../../packages/core/core_shared
  core_ui:
    path: ../../packages/core/core_ui
  design_system_shared:
    path: ../../packages/design_system/design_system_shared
  design_system_ui:
    path: ../../packages/design_system/design_system_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
