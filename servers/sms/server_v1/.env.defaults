# Defaults não-sensíveis, agnósticos de ambiente
# Lido pelo envied (build-time) — seguro para versionar
# Apenas os campos da classe Env (lib/config/env/env.dart)
# Secrets e DB são injetados via Platform.environment em runtime (ver container/.env)

BACKEND_PATH_API=/api/v1
SERVER_PORT=8080
ENABLE_DOCS=false

# Rate limiting / Auth
ACCESS_TOKEN_EXPIRES_MINUTES=15
REFRESH_TOKEN_EXPIRES_DAYS=7
MAX_LOGIN_ATTEMPTS_PER_ACCOUNT=5
MAX_LOGIN_ATTEMPTS_PER_IP=10
ACCOUNT_LOCKOUT_MINUTES=30
IP_BLOCK_MINUTES=15
