# Porta que o servidor Dart/shelf escuta internamente dentro do container
SERVER_PORT=8080
# Porta que será exposta na máquina host (mapeada para SERVER_PORT)
HOST_PORT=8080

# Conexão com o Banco
# Nome do Container
DB_HOST=name_host
DB_PORT=5432

# Ambiente da aplicação (development, staging, production)
# Nota: Para dev, use docker-compose.dev.yml que define ENVIRONMENT=development
# Este arquivo é compartilhado, então deixe comentado ou use 'development' como padrão
ENVIRONMENT=development

# Credenciais EXCLUSIVAS do SMS
DB_NAME=db_name
DB_USER=user_name
DB_PASS=password
