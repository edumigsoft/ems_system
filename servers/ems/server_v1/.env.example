# Template para server_v1/.env (desenvolvimento local sem Docker)
# Copie para .env, preencha os valores e NÃO versione o .env
# Carregue com: export $(grep -v '^#' .env | xargs) && dart run bin/server.dart

# Ambiente
ENVIRONMENT=development

# Database — conecta ao PostgreSQL local ou via container
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_ems
DB_USER=db_username
DB_PASS=db_password

# Secrets de runtime (obrigatórios — ver bin/server.dart e lib/config/injector.dart)
JWT_KEY=change_to_secure_random_key
API_KEY=change_to_secure_random_key
VERIFICATION_LINK_BASE_URL=http://localhost:8181/api/v1/auth/verify

# Email (opcional em dev — use Mailpit ou similar em localhost:1025)
EMAIL_SERVICE_HOST=localhost
EMAIL_SERVICE_PORT=1025
EMAIL_SERVICE_API_KEY=not-needed-dev
