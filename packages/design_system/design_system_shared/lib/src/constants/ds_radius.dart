class DSRadius {
  static const double small = 4.0; // Badges, tags pequenos
  static const double medium = 8.0; // Elementos internos, ícones
  static const double large = 12.0; // Containers médios, navegação
  static const double xlarge = 16.0; // Cards, containers padrão
  static const double xxlarge = 24.0; // Containers grandes, páginas

  // Radius específico para input fields (12.0 para consistência visual)
  static const double input = 12.0;
}
