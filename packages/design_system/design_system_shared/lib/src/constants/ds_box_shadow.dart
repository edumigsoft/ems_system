class DSBoxShadow {
  static const extraSmall = (
    blurRadius: 4.0,
    // spreadRadius: 1.0,
    offset: (dx: 0.0, dy: 2.0),
  );

  static const small = (
    blurRadius: 12.0,
    spreadRadius: 2.0,
    offset: (dx: 2.0, dy: 4.0),
  );

  static const medium = (
    blurRadius: 16.0,
    spreadRadius: 2.0,
    offset: (dx: 0.0, dy: 4.0),
  );

  static const large = (
    blurRadius: 24.0,
    spreadRadius: 3.0,
    offset: (dx: 0.0, dy: 8.0),
  );
}
