# Auth Client

Cliente HTTP e Services client-side para autenticação.

## Funcionalidades
- AuthApiService (Retrofit)
- AuthService (Wrapper)
- AuthInterceptor (Dio) para injeção automática de token e refresh.
- TokenStorage (Secure Storage).
