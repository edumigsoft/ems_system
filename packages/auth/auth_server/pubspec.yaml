name: auth_server
description: Server implementation for Authentication.
publish_to: none
version: 1.1.0

# repository: https://github.com/edumigsoft/ems_system_core

resolution: workspace

environment:
  sdk: ^3.10.7

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  drift: ^2.30.0
  core_shared: ^1.1.0
  core_server: ^1.1.0
  open_api_shared: ^1.1.0
  user_shared: ^1.1.0
  user_server: ^1.1.0
  auth_shared: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
  build_runner: ^2.10.5
  drift_dev: ^2.30.0
  mocktail: ^1.0.4
