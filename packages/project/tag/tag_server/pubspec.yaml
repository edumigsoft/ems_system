name: tag_server
description: Server implementation for tag management feature
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  tag_shared:
    path: ../tag_shared
  core_shared:
    path: ../../../core/core_shared
  core_server:
    path: ../../../core/core_server
  drift: ^2.23.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  postgres: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
  drift_dev: ^2.23.0
  build_runner: ^2.10.5
