name: tag_shared
description: Core domain logic and entities for tag management feature
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  core_shared:
    path: ../../../core/core_shared
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
