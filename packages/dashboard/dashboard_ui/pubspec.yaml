name: dashboard_ui
description: Dashboard UI Package — agrega widgets de qualquer módulo registrado.
publish_to: none
version: 1.1.0

resolution: workspace

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  get_it: ^9.2.0
  core_shared: ^1.1.0
  core_ui: ^1.1.0
  design_system_shared: ^1.1.0
  design_system_ui: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
